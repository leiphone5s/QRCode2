<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lei.qrcode">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_192x192"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_192x192"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/title_activity_login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".RegisterActivity" />
        <activity android:name="view.AboutUs" >
            <intent-filter>
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name="view.Resetpwd" />
        <activity android:name="view.AbsenseWarn" />
        <activity android:name="view.AbsenseRecord" />
        <activity android:name="view.GetbackPwd" />
        <activity android:name="view.ModifyPersonalInfo" />
        <activity android:name="view.AbsenseConstantActivity" />

        <activity android:name="view.PersonalSettings" />

        <activity
            android:name="view.RegisterSelectWheel"
            android:theme="@style/MyToolsAddAvatar" />

    </application>

</manifest>