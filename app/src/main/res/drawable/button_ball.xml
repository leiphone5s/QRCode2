<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"> android:shape="rectangle"> <!-- 填充的颜色 -->
    <corners android:radius="80dp" />
    <solid android:color="@color/dark" />
    <size
        android:width="100dp"
        android:height="20dp" />
    <stroke
        android:width="2dp"
        android:color="@color/blue" />
</shape>